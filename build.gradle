plugins {
    id 'java'
}

repositories {
    jcenter()
}

dependencies {
    // Нет дополнительных зависимостей для этого примера
}

task runHelloWorld(type: JavaExec) {
    main = 'org.example.Main'
    classpath = sourceSets.main.runtimeClasspath
}